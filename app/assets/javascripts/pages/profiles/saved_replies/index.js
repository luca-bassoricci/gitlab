import $ from 'jquery';
import GLFrom from '~/gl_form';
import ZenMode from '~/zen_mode';

new GLFrom($('.js-saved-reply-form')); // eslint-disable-line no-new
new ZenMode(); // eslint-disable-line no-new
